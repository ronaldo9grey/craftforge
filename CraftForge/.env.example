# 前端环境变量模板（入 git，作为新成员/服务器构建参考）
# 复制为 .env.local（开发）或 .env.production（生产构建），并填入真实值

# 后端 API 基础路径：生产环境推荐 /api（同源由 nginx 代理）
VITE_API_BASE_URL=/api
# 与后端 .env 的 APP_TOKEN 保持一致
VITE_APP_TOKEN=请填入 APP_TOKEN
